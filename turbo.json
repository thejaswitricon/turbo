{
  "$schema": "https://turborepo.org/schema.json",
  "packages": {
    "@mono/config-release-it": {
      "release": {
        "command": "echo 'Skipping release-it command for @mono/config-release-it package'"
      }
    }
  },
  "pipeline": {
    "start": {
      "cache": false
    },
    "release": {
      "dependsOn": ["^release"],
      "outputMode": "new-only"
    }
  }
}
